Proteus Directory Structure
============================

Here's the structure of the Proteus Application, avoiding some directories like the content
of the `resources` directory or the `doc` directory.

.. literalinclude:: proteus_structure.txt
   :language: none